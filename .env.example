# Supabase Dashboard → Connect → Session pooler connection string.
# Use this for the persistent Node/Render server when the direct IPv6 endpoint
# is unavailable. This value must never be exposed to the browser.
SUPABASE_DB_URL=postgresql://postgres.PROJECT_REF:PASSWORD@aws-REGION.pooler.supabase.com:5432/postgres
